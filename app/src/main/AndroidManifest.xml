<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.ESCOMAPPCELULAR">
        <activity android:name=".Mapa" />
        <activity android:name=".ConsultarActivity" />
        <activity android:name=".AreasListaActivity" android:theme="@style/Theme.ESCOMAPPCELULAR.Light"/>
        <activity android:name=".AreaDetalleActivity" android:theme="@style/Theme.ESCOMAPPCELULAR.Light"/>
        <activity
            android:name=".ProfesoresListaActivity"
            android:theme="@style/Theme.ESCOMAPPCELULAR.Light"/>
        <activity android:name=".ProfesorDetalleActivity"/>
        <activity android:name=".BecasListaActivity"/>
        <activity android:name=".BecaDetalleActivity"/>
        <activity
            android:name=".ChatbotActivity"
            android:label="ESCOMbot"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/app_name"
            android:theme="@style/Theme.ESCOMAPPCELULAR">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"

            android:value="@string/google_maps_key" />

    </application>

</manifest>